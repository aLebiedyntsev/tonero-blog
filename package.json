{
  "name": "tonero-blog",
  "version": "1.0.0",
  "description": "AI-powered blog generator for tonero.app",
  "private": true,
  "scripts": {
    "generate": "node generate.js",
    "build": "node build.js",
    "gen-and-build": "node generate.js && node build.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "openai": "^4.47.0"
  },
  "engines": {
    "node": ">=18"
  }
}
